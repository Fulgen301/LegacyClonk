name: Autobuild

on:
  push:
    branches-ignore: '*'
    tags-ignore: '*'
  pull_request:
    branches: '*'

defaults:
  run:
    shell: bash

jobs:
  Autobuild:
    uses: './.github/workflows/build.yml'
